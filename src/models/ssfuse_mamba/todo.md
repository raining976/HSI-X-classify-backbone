思路：
- pca用hilbert3d扫描
- x 空间mamba

建模后双模态交互，这样可以堆叠。

pca 3d扫描后要恢复为特征

如果用协方差自注意力进行特征融合；还要考虑空间上如何融合   



能不能在两个分支建模时，改成这样：线性层处理->dwconv->sigmoid激活->输入到一个交互的选择性扫描模块
这个交互的选择性扫描模块的核心机制是交换两个模态生成的B矩阵，然后进行扫描，相当于让这两个模态进行了深度建模，然后输出两个序列，分别再经过ln，linear，然后与输入残差连接，这样还能不能再恢复为feature map了，后续经过cca融合模块，